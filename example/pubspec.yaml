name: popover_example
description: Popover Example.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  elegant_spring_animation: ^2.0.2
  flutter:
    sdk: flutter

  popover:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
